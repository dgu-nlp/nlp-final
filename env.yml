name: nlp_final
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - torch
      - torchvision
      - torchaudio
      - tqdm==4.58.0
      - requests==2.25.1
      - importlib-metadata==3.7.0
      - filelock==3.0.12
      - sklearn==0.0
      - tokenizers==0.20
      - explainaboard_client==0.0.7
      - einops==0.8.0
      - transformers==4.46.3
      - sacrebleu==2.5.1
      ### knn_gpt용 추가 라이브러리
      ### 기본 프로젝트 1과 2에서는 사용하지 않았습니다.
      - faiss-gpu # 데이터스토어 구축, 검색 시 사용
      - datasets # wikitext 데이터셋 다운로드 시 사용